pipeline {
    agent any
    
    stages() {
        stage('Test') {
            steps {
                sh 'ls -al'
            }
        }

        stage('Docker Build') {
            steps {
                sh 'docker build -t git_action .'
            }
        }
    }
}